[tool.poetry]
name = "poetry"
version = "0.1.0"
description = ""
authors = ["Ranel Tammel <raneltam@ad.helsinki.fi>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
invoke = "^2.2.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
